<h1>New Comment</h1>


<%= form_for @comment do |c| %>
<%= c.text_area :body %> <br />
<%= c.select :post_id, Post.pluck(:name) %> <br />
<%= c.submit %> 
<% end %>

<%= link_to 'Back', comments_path %>
